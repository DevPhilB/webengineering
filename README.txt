
Aufgabe 1a) bis 1d) wurden bearbeitet und im Code makiert.
Aufgabe 2a) makiert. // testDijkstra funktioniert.

Die zusätzliche Costen berechnung für die Knoten(holen von Depatures) 
ist in der Funktion getNextDepatureCost(...), diese liefert nur 0 weil es sonst nicht geht.
// 


Ergebnis von 2b) in der ergebnis.txt
Um Ergebnisse zu bekommen benutzten wir die mysearch.php den RouteFormatter brachten wir nicht zum laufen.
